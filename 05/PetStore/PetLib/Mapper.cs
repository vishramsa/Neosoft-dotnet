namespace PetLib
{
    public class Mapper
    {
    /*
        public static PetData.Cat Map(PetLib.Cat cat){
            return new PetData.Cat(){
                Id=cat.Id,
                CatType=Map(cat.CatType),
                Dob=cat.Dob,
                Gender=Map(cat.Gender),
                Name=cat.Name,
                Weight=cat.Weight
            };
        }
        public static PetData.CatType Map(PetLib.CatType catType){
            return (PetData.CatType)catType;
        }
        public static PetLib.CatType Map(PetData.CatType catType){
            return (PetLib.CatType)catType;
        }
        public static PetData.Gender Map(PetLib.Gender gender){
            return (PetData.Gender)gender;
        }
        public static PetLib.Gender Map(PetData.Gender gender){
            return (PetLib.Gender)gender;
        }
        public static PetLib.Cat Map(PetData.Cat cat){
            return new Cat(){    
                Id=cat.Id,
                CatType=Map(cat.CatType),
                Dob=cat.Dob,
                Gender=Map(cat.Gender),
                Name=cat.Name,
                Weight=cat.Weight
            };
        }
    */
    }
}